{
    "jobs": [
        {
            "functionLocation": "/jobs.js", 
            "functionName": "runScheduledTasks",
            "description": "Checks and Runs Scheduled Tasks",
            "executionConfig": {
                "cronExpression": "*/5 * * * *"
            }
        },
        {
            "functionLocation": "/jobs.js", 
            "functionName": "fetchAndSaveBasicJobsFast",
            "description": "Fetches jobs data from Smart Recruiters API and updates ALL existing jobs",
            "executionConfig": {
                "cronExpression": "*/10 * * * *"
            }
        }
    ]
}
